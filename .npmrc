# @cloudflare/next-on-pages 1.x has drifted peer ranges (wrangler v4 wants
# workers-types v5, the adapter wants v4 — both optional). Skip strict peer
# resolution; required both locally and on the Cloudflare Pages build CI.
legacy-peer-deps=true
