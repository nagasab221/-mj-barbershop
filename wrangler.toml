# Cloudflare Pages configuration.
# Read automatically when the repo is connected to a Pages project —
# provides the Node.js compatibility flag the edge runtime needs.
name = "mj-barbershop"
compatibility_date = "2025-03-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"
